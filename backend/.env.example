# ===========================================
# Assistente Profissional - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores

# Servidor
PORT=3000
NODE_ENV=development

# Banco de Dados (PostgreSQL)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/assistente_db?schema=public"

# Redis (para filas)
REDIS_URL="redis://localhost:6379"

# JWT Secret (gere um valor aleatório seguro)
JWT_SECRET="sua-chave-secreta-super-segura-aqui"

# Google OAuth (Gmail API)
# Obtenha em: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="seu-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="seu-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/auth/google/callback"

# Anthropic API (Claude para resumos)
# Obtenha em: https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# OpenAI API (alternativa ao Claude + Whisper para transcrição)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Configurações de Resumo
MAX_SUMMARY_TOKENS=150
SUMMARY_MODEL="claude-3-haiku-20240307"

# Configurações de Auto-Reply
DEFAULT_AUTO_REPLY_MESSAGE="Recebi sua mensagem! No momento estou em atendimento, mas retorno assim que possível. Obrigado pela compreensão."
